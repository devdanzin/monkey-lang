# HEARTBEAT.md

# Memory maintenance (every few days, rotate with other checks):
# - Review recent memory/YYYY-MM-DD.md files
# - Check if MEMORY.md needs updating (new facts, stale info)
# - Ensure entity pages are current
# - Reindex memory if new files were added: openclaw memory index
