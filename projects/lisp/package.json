{
  "name": "lisp",
  "version": "0.1.0",
  "type": "module",
  "description": "Lisp interpreter — reader, evaluator, special forms, closures, macros",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
