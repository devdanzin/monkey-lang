{
  "name": "http-server",
  "version": "1.0.0",
  "description": "HTTP/1.1 server from scratch — raw TCP, request parsing, routing, middleware",
  "type": "module",
  "scripts": {
    "test": "node --test src/**/*.test.js"
  }
}
