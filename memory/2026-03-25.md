# 2026-03-25 — Session B (2:15 PM – ongoing)

## Language Features Added
- Compound assignment (+=, -=, *=, /=, %=)
- String multiplication ("ha" * 3)
- String comparisons (==, !=, <, >)
- Fixed 11 broken tests: <=, >=, &&, || infix parsers + compiler
- For-loops (C-style)
- For-in iteration (arrays + strings)
- Break and continue
- String interpolation (`hello ${name}`)
- Negative indexing (arr[-1])
- Escape sequences (\n \t \\ \")
- Array/hash mutation (arr[i] = val)
- Compound index assignment (arr[i] += val)
- Mutable closures (OpSetFree)
- Else-if chains
- Default function parameters
- Null keyword literal
- Array/string slicing (arr[1:3])
- Ternary operator (a ? b : c)

## Stdlib
- Modernized with for/for-in
- Added: sum, max, min, zip, enumerate, flat, sort

## Documentation/Blog
- Blog: "Growing a Language" (language design decisions)
- Show HN draft written
- README updated, language reference expanded
- Playground rebuilt 3x with new features
- 5 example programs (mandelbrot, fibonacci, sorting, closures, fizzbuzz)

## Stats
- 449 tests, 0 failures
- ~9x JIT aggregate speedup
- JIT bug with nested function loops investigated but not fixed (pre-existing)

## JIT Bug
- Root cause: inner-loop traces inside functions corrupt stack frame
- Added try/catch safety around trace execution as workaround
- Needs deeper architectural fix (trace context isolation)
