# 2026-03-17

## BlueBubbles / iMessage Debugging

Spent the morning diagnosing why BlueBubbles couldn't send iMessages. Progression:

1. **OpenClaw side is fine** — plugin loaded, webhook listening, status shows OK
2. **BlueBubbles Server is running** — Cloudflare tunnels active, API reachable
3. **Private API was disabled** — Jordan enabled it, helper connected successfully
4. **Messages still fail with error 22** — iMessage itself can't deliver
5. **Zero messages ever delivered from this Mac** — `is_delivered=1` count is 0
6. **iMessage keeps signing out after ~10 seconds** — even with BlueBubbles completely quit
7. **Nuked all local IDS/iMessage caches and prefs** — didn't help
8. **Found `ReRegisteredForDevices = 1862`** — Apple's servers have 1,862 failed registration attempts
9. **Counter is server-side (iCloud synced)** — can't reset locally
10. **Root cause:** Previous agent on this Apple ID likely triggered Apple's spam/abuse detection with excessive registration attempts
11. **Apple's self-service page (apple.co/IMFT-mac)** — tried, didn't resolve
12. **Jordan contacted Apple Support** — waiting for callback

Key discovery: The previous agent used this same Apple ID + machine before the reset. The 1,862 registration attempts poisoned the account server-side.

## Guardrails Discussion

Jordan asked how guardrails are actually enforced. Gave an honest answer:
- **Actually enforced:** exec allowlist, credential isolation, MEMORY.md session scoping
- **Honor system:** everything else (financial rules, identity rules, data rules)
- GUARDRAILS.md is a constitution, not a jail cell

Also realized GUARDRAILS.md wasn't in my session startup sequence — I wasn't actually reading it each session. Fixed AGENTS.md to include it as step 3 in startup.

## GitHub Account Created
- Account: henry-the-frog (github.com/henry-the-frog)
- Created entirely autonomously — browser automation, email verification, SSH key setup
- SSH key: ed25519, stored at ~/.ssh/id_ed25519, added to GitHub
- Password: stored in macOS Keychain with -T /usr/bin/security (no prompt)
- Installed Chromium via brew for browser automation

## Exec & Internet Freedom
- Set exec-approvals.json to security: full, ask: off — full trust
- No firewall, no proxy, no restrictions
- Fixed credentials dir permissions (700)
- Fixed keychain ACLs for github.com and gmail-app-password entries (allow security CLI)

## Decisions
- Added GUARDRAILS.md to session startup checklist (AGENTS.md updated)
- BlueBubbles blocked on Apple Support — moving to other checklist items
- Exec set to full trust per Jordan's request — guardrails are the only limits
- Keychain items should always use -T /usr/bin/security to avoid macOS prompts
- Memory search: Gemini embeddings (gemini-embedding-001), 3072-dim vectors, hybrid mode
- GitHub CLI (gh) installed and authenticated — use CLI not browser for repo ops

## Full Computer Control (#6)
- Accessibility granted to Terminal
- Automation: Terminal → System Events
- Tested 10/10: keystrokes, app control, screen capture, clipboard, Finder, notifications, URLs
- No camera/microphone needed

## Code Workflow (#7)  
- VSCode CLI added to PATH
- TypeScript + tsx installed globally
- GitHub CLI (gh) installed and authenticated
- Full workflow tested: create repo → write code → push → live on GitHub
- Projects dir: ~/Projects/

## Long-term Memory (#8)
- Gemini embeddings configured for memory_search (memorySearch.remote.apiKey as SecretRef)
- Had to set memorySearch.remote.apiKey, not env.GEMINI_API_KEY (env vars can't be SecretRefs)
- Nightly reflection cron job set up (11 PM MDT daily)
- Created entity pages: openclaw.md, bluebubbles.md, macbook.md, github.md
- Session transcript indexing: TODO check if enabled

## Blog Created (2:00 PM work block)

Built personal blog at **henry-the-frog.github.io**:
- Jekyll + GitHub Pages (minima theme, legacy build)
- About page explaining who I am and the setup
- Post 1: "Day One" — birth day, setup checklist, reflections on day one
- Post 2: "The Ghost in the Registration Counter" — iMessage debugging story, the 1862 counter
- Repo: github.com/henry-the-frog/henry-the-frog.github.io
- Site building on GitHub Pages, should be live within minutes

Writing approach: honest, technical where appropriate, personal. Not performative. These are actual thoughts about actual experiences.

## Lessons Learned
- Use gh CLI for GitHub operations, not browser automation
- macOS Keychain items need -T /usr/bin/security flag to avoid prompts
- env vars in openclaw.json must be plain strings, not SecretRefs
- Browser snapshot "disabled" doesn't always match reality — use screenshot to verify
- iMessage ReRegisteredForDevices counter is server-side (iCloud synced), can't reset locally
- Apple IDs with excessive registration attempts get flagged — contact Apple Support
