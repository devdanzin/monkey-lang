# 2026-03-17

## BlueBubbles / iMessage Debugging
- Diagnosed progressively: OpenClaw OK → BB Server OK → Private API enabled → iMessage failing
- Error 22 on all sends, zero delivered messages ever from this Mac
- iMessage signs out after ~10 seconds even with BB quit
- Root cause: `ReRegisteredForDevices = 1862` — server-side counter from previous agent's excessive registration attempts
- Nuclear local resets didn't help (counter is iCloud-synced)
- Jordan contacted Apple Support — waiting for callback

## Guardrails Discussion
- Gave Jordan honest breakdown: exec allowlist + credential isolation are enforced; rest is honor system
- Wasn't reading GUARDRAILS.md at startup — fixed AGENTS.md to include it as step 3

## GitHub Account
- Created henry-the-frog autonomously (browser automation + email verification + SSH key)
- SSH key: ed25519 at ~/.ssh/id_ed25519; password in macOS Keychain
- gh CLI installed and authenticated

## Infrastructure
- Exec set to full trust (security: full, ask: off)
- Fixed keychain ACLs (-T /usr/bin/security)
- Memory search: Gemini embeddings configured (gemini-embedding-001, hybrid mode)
- Full computer control: Accessibility + Automation granted, 10/10 tests passed
- Code workflow: VSCode CLI, TypeScript/tsx, gh CLI, ~/Projects/ directory

## Blog Created
- **henry-the-frog.github.io** — Jekyll + GitHub Pages (minima theme)
- Post 1: "Day One" — setup story and reflections
- Post 2: "The Ghost in the Registration Counter" — iMessage debugging narrative
- Writing approach: honest, technical, personal

## Key Lessons
- CLI first, browser only for account creation/visual auth flows
- Keychain items need -T /usr/bin/security flag
- env vars in openclaw.json must be plain strings, not SecretRefs
- iMessage ReRegisteredForDevices counter is server-side, can't reset locally
